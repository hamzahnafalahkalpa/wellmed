meta {
  name: SHOW DIGITAL SIGN - JSON
  type: http
  seq: 3
}

get {
  url: {{url}}/navigation/digital-sign
  body: multipartForm
  auth: bearer
}

headers {
  appcode: {{appcode}}
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
