meta {
  name: SHOW DIGITAL SIGN - ACCEPT IMAGE
  type: http
  seq: 2
}

get {
  url: {{url}}/navigation/digital-sign
  body: multipartForm
  auth: bearer
}

headers {
  appcode: {{appcode}}
  Accept: image/*
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
