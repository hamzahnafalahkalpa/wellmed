meta {
  name: STORE COMPOSITION UNIT
  type: http
  seq: 2
}

post {
  url: {{url}}/setting/item-management/composition-unit
  body: json
  auth: bearer
}

headers {
  appcode: {{appcode}}
  accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "id" : null,
      "parent_id": null,
      "name" : "gram"
  }
}

script:post-response {
  let data = res.getBody()
  bru.setVar('composition_unit_id',data.data.id)
}

settings {
  encodeUrl: true
}
