meta {
  name: DELETE PATIENT TYPE SERVICE
  type: http
  seq: 3
}

delete {
  url: {{url}}/setting/faskes-service/patient-type-service/:id
  body: json
  auth: bearer
}

params:path {
  id: {{patient_type_service_id}}
}

headers {
  Accept: application/json
  appCode: {{appcode}}
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
