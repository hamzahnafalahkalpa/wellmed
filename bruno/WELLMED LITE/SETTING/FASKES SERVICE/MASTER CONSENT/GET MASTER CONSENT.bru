meta {
  name: GET MASTER CONSENT
  type: http
  seq: 1
}

get {
  url: {{url}}/setting/faskes-service/master-informed-consent
  body: none
  auth: bearer
}

headers {
  appcode: {{appcode}}
  accept: application/json
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  bru.setVar('service_label_id',res.getBody().data[0].id)
}

settings {
  encodeUrl: true
}
