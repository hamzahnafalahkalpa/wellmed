meta {
  name: DELETE TARIF COMPONENT
  type: http
  seq: 4
}

delete {
  url: {{url}}/setting/finance/tariff-component/:id
  body: json
  auth: bearer
}

params:path {
  id: {{tariff_component_id}}
}

headers {
  appCode: {{appcode}}
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}

docs {
  Gets information about the authenticated user.
}
