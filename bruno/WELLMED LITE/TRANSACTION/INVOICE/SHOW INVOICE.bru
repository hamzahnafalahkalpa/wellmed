meta {
  name: SHOW INVOICE
  type: http
  seq: 2
}

get {
  url: {{url}}/transaction/invoice/:id
  body: none
  auth: bearer
}

params:path {
  id: {{invoice_id}}
}

headers {
  AppCode: {{appcode}}
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
