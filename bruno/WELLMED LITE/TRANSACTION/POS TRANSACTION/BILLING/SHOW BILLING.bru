meta {
  name: SHOW BILLING
  type: http
  seq: 3
}

get {
  url: {{url}}/transaction/point-of-sale/:transaction_id/billing/:id
  body: none
  auth: bearer
}

params:path {
  transaction_id: {{transaction_id}}
  id: {{billing_id}}
}

headers {
  AppCode: {{appcode}}
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
