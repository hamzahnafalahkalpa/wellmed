meta {
  name: SHOW VISIT EXAMINATION
  type: http
  seq: 3
}

get {
  url: {{url}}/patient-emr/patient/:patient_id/visit-patient/:visit_patient_id/visit-registration/:visit_registration_id/visit-examination/:id
  body: none
  auth: bearer
}

params:path {
  patient_id: {{patient_id}}
  visit_patient_id: {{visit_patient_id}}
  visit_registration_id: {{visit_registration_id}}
  id: {{visit_examination_id}}
}

headers {
  appcode: {{appcode}}
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
